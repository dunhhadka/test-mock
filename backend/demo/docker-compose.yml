version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-management
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: test
    ports:
      - "3306:3306"